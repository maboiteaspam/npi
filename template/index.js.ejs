<% dependencies.split(/\s/).forEach(function (module) {
  if (module) {
    var jsModule = module.match(/([^/]+)$/)[1]
    jsModule = jsModule.replace(/[-]/g, '_');
    print("var "+jsModule+" = require('"+module+"')\n")
  }
}) %>
<% devDependencies.split(/\s/).forEach(function (module) {
  if (module) {
    var jsModule = module.match(/([^/]+)$/)[1]
    jsModule = jsModule.replace(/[-]/g, '_');
    print("var "+jsModule+" = require('"+module+"')\n")
  }
}) %>

var yourModule = function () {

}

module.exports = yourModule;